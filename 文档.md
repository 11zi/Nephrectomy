# 后端接口与数据

## table of table

| 表名     | 主键        | arg1               | arg2           | arg3           | arg4               | arg5     | arg6           | arg7           | arg8             | arg9             | arg10 | arg11 | arg12          | arg13 | arg14 | arg15 | arg16 | arg17    | arg18          | arg19 | arg20          | arg21          | arg22  | arg23        | arg24    | arg25    | ..  |
| -------- | ----------- | ------------------ | -------------- | -------------- | ------------------ | -------- | -------------- | -------------- | ---------------- | ---------------- | ----- | ----- | -------------- | ----- | ----- | ----- | ----- | -------- | -------------- | ----- | -------------- | -------------- | ------ | ------------ | -------- | -------- | --- |
| 用户     | guid        | 昵称               | 坐标(房间-uid) | 状态           | 赞                 | 相册     | 同框(用户-uid) | 账户状态       | 称号             | 性别             | 生日  | 年龄  | 住址(房间-uid) | 标签  | 爱好  | 好友  | 邮箱  | 个人网站 | 社区(房间-uid) | 金钱  | 关注(用户-uid) | 粉丝(用户-uid) | 访问量 | 最后登录时间 | 注册时间 | 在线时长 |
| 房间     | uid         | 名称               | 订阅(用户-uid) | 当前成员       | 当前人数(用户-uid) | 建造时间 | 标签           | 成员(用户-uid) | 拥有者(用户-uid) | 创建者(用户-uid) | 简介  |
| 聊天记录 | saving_time | 消息(id)           |
| 消息     | guid        | 所属房间(房间-uid) | 时间戳(服务器) | 用户(用户-uid) | 内容               |

# Vue组件

文件路径/文件名 `./src/App.vue`
根组件，所有组件挂载在这里

## 侧边栏

文件路径/文件名 `./src/views/SideBar.vue`
侧边栏可以展开和折叠，左上角会显示用户头像作为开关按钮

### 基础功能-用户编辑

### 基础功能-房间信息

### 基础功能-房间列表

### 基础功能-金

### 基础功能-动态

### 扩展功能-位置交互

### 扩展功能-背包

### 扩展功能-小游戏

### 基础功能-系统设置

## 内容

文件路径/文件名 `./src/views/Content.vue`
内容页面默认打开聊天室-空间站

### 聊天室

文件路径/文件名 `./src/views/Content/Chat/ChatRoom.vue`
关联组件：输入栏，消息
聊天室页面无法关闭，默认位置为空间站

- 底部有输入栏组件
- 背景可变为视频

#### 消息组件

文件路径/文件名 `./src/views/Content/Chat/Message.vue`
每一条消息是一个单独组件
消息内容会当作markdown解析、渲染
markdown是另一个组件，因为所有可编辑内容都会用到markdown

#### 消息-状态更新

文件路径/文件名 `./src/views/Content/Chat/StateMessage.vue`
状态更新显示在中间

#### 消息-指令

文件路径/文件名 `./src/views/Content/Chat/CommandMessage.vue`
指令型消息不显示，但运行脚本

### 次级内容

文件路径/文件名 `./src/views/Content/TemporaryPage.vue`
聊天室、系统公告、查看用户资料、房间资料、房间列表等功能
关闭后会返回上一级，直至回到房间（或者它只是简单的覆盖在之前的页面上）

## 卡片组件

文件路径/文件名 `./src/views/FloatingCard.vue`
卡片组件悬浮在内容页面上，可到处拖动

## 其他组件

嵌入其他组件

### 输入栏

文件路径/文件名 `./src/views/OtherComponent/InputAssistant.vue`
输入栏可展开，底部有便签、预置表情、媒体文件上传等功能

### Markdown解析块

文件路径/文件名 `./src/views/OtherComponent/Markdown.vue`
返回解析后的结果

- 组件宽度受父节点的大小影响，高度为自身高度
- 支持音频、视频插入
- 出于安全考虑不支持html
