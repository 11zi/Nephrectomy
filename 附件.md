好的，既然你要直接给 Codex 实现，那我们就不绕弯子了。下面是一份**“可直接翻译成代码”的规则说明书**，附带关键数值和伪代码。

---

## 一、核心目标（给 Codex 的约束条件）

> **每只股票平均每 24 小时崩盘 1 次**（以每分钟更新 1 次计，即平均每 1440 次更新触发 1 次崩盘）

在这个约束下，崩盘价的追赶速度被严格确定。

---

## 二、最终规则（可直接实现版）

### 2.1 全局参数

| 参数名                    | 值     | 说明                                 |
| :------------------------ | :----- | :----------------------------------- |
| `UPDATE_INTERVAL_SECONDS` | 60     | 每次更新间隔（秒）                   |
| `MEAN_REVERSION_TARGET`   | 1.0    | 均值回归基准（固定）                 |
| `MEAN_REVERSION_STRENGTH` | 0.10   | 每次回归 10%                         |
| `RANDOM_VOLATILITY`       | 0.02   | 单次随机波动 ±2%                     |
| `RANDOM_BIAS`             | 0.001  | 额外偏移 ±0.001                      |
| `INITIAL_PRICE`           | 1.0    | 初始股价                             |
| `INITIAL_CRASH_PRICE`     | 0.10   | 初始崩盘价                           |
| `INITIAL_PEAK_PRICE`      | 10.0   | 初始高位价                           |
| `CRASH_PRICE_RATIO`       | 1.1    | 崩盘价 = 近期最低 × 1.1              |
| `PEAK_PRICE_RATIO`        | 0.9    | 高位价 = 近期最高 × 0.9              |
| `RECENT_WINDOW`           | 120    | 近期窗口（最近 120 次更新 ≈ 2 小时） |
| `CRASH_CATCHUP_SPEED`     | 0.02   | 崩盘价追赶速度 2%                    |
| `PEAK_CATCHUP_SPEED`      | 0.02   | 高位价追赶速度 2%                    |
| `MIN_STEP`                | 0.0005 | 最小更新步长                         |
| `BULL_CANCEL_STREAK`      | 4      | 连续下跌 4 次取消牛股                |
| `BULL_CANCEL_PULLBACK`    | 0.5    | 取消牛股时高位价向当前价靠拢 50%     |

---

### 2.2 每只股票的状态变量

```python
{
    "price": 1.0,                # 当前股价
    "crash_price": 0.1,          # 当前崩盘价
    "peak_price": 10.0,          # 当前高位价
    "is_bull": False,            # 是否牛股
    "is_crashed": False,         # 是否已崩盘（只能卖出）
    "price_history": [],         # 最近 N 次价格记录（用于计算近期最低/最高）
    "consecutive_down": 0,       # 连续下跌次数
    "prev_price": 1.0,           # 上一次价格
}
```

---

### 2.3 每分钟更新逻辑（伪代码）

```python
def update_stock(stock):
    # ========================================
    # 第一步：如果已崩盘，价格继续下跌（死亡螺旋）
    # ========================================
    if stock.is_crashed:
        # 崩盘后仍然更新价格，但只能卖出
        stock.price = next_price(stock.price)
        # 崩盘价继续追赶，确保永远回不来
        stock.crash_price = update_crash_price(stock)
        return

    # ========================================
    # 第二步：正常价格更新
    # ========================================
    old_price = stock.price
    stock.price = next_price(stock.price)

    # 记录价格历史（用于近期最低/最高）
    stock.price_history.append(stock.price)
    if len(stock.price_history) > RECENT_WINDOW:
        stock.price_history.pop(0)

    # ========================================
    # 第三步：更新崩盘价（只升不降）
    # ========================================
    stock.crash_price = update_crash_price(stock)

    # ========================================
    # 第四步：崩盘判定（当前价 < 崩盘价）
    # ========================================
    if stock.price < stock.crash_price:
        stock.is_crashed = True
        stock.is_bull = False
        # 崩盘后崩盘价和高位价冻结（不再变化）
        # 但价格继续下跌
        return

    # ========================================
    # 第五步：更新高位价（只降不升）
    # ========================================
    stock.peak_price = update_peak_price(stock)

    # ========================================
    # 第六步：牛市判定（当前价 > 高位价）
    # ========================================
    if stock.price > stock.peak_price:
        stock.is_bull = True

    # ========================================
    # 第七步：牛股连续下跌判定（取消牛股）
    # ========================================
    if stock.is_bull:
        if stock.price < stock.prev_price:
            stock.consecutive_down += 1
        else:
            stock.consecutive_down = 0

        if stock.consecutive_down >= BULL_CANCEL_STREAK:
            stock.is_bull = False
            stock.consecutive_down = 0
            # 取消牛股时，高位价向当前价靠拢 50%
            stock.peak_price = stock.peak_price * 0.5 + stock.price * 0.5

    # 记录当前价格作为下一次的"上一次"
    stock.prev_price = stock.price
```

---

### 2.4 价格更新函数

```python
def next_price(current_price):
    # 均值回归
    reversion = (1.0 - current_price) * 0.10

    # 随机噪声：±2% ± 0.001
    noise_percent = random.uniform(-0.02, 0.02)
    noise_bias = random.uniform(-0.001, 0.001)
    noise = current_price * noise_percent + noise_bias

    new_price = current_price + reversion + noise

    # 价格下限保护（但不能低于0，否则崩盘永远触发不了）
    return max(new_price, 0.001)
```

---

### 2.5 崩盘价更新函数（核心）

```python
def update_crash_price(stock):
    # 已崩盘则冻结
    if stock.is_crashed:
        return stock.crash_price

    # 计算近期最低价
    recent_low = min(stock.price_history) if stock.price_history else stock.price

    # 目标崩盘价 = 近期最低 × 1.1（追赶逻辑）
    target = recent_low * CRASH_PRICE_RATIO

    # 只升不降，缓慢追赶
    if target > stock.crash_price:
        diff = target - stock.crash_price
        step = max(diff * CRASH_CATCHUP_SPEED, MIN_STEP)
        return stock.crash_price + step
    else:
        # 如果目标低于当前崩盘价，保持不变
        return stock.crash_price
```

---

### 2.6 高位价更新函数（核心）

```python
def update_peak_price(stock):
    # 已崩盘则冻结
    if stock.is_crashed:
        return stock.peak_price

    # 计算近期最高价
    recent_high = max(stock.price_history) if stock.price_history else stock.price

    # 目标高位价 = 近期最高 × 0.9（追赶逻辑）
    target = recent_high * PEAK_PRICE_RATIO

    # 只降不升，缓慢追赶
    if target < stock.peak_price:
        diff = stock.peak_price - target
        step = max(diff * PEAK_CATCHUP_SPEED, MIN_STEP)
        return stock.peak_price - step
    else:
        # 如果目标高于当前高位价，保持不变
        return stock.peak_price
```

---

### 2.7 近期窗口大小验证（确保 24 小时崩盘）

在你的均值回归参数下，股价跌到崩盘价的时间由崩盘价的追赶速度决定。

**当前参数下**：

- 近期窗口 `RECENT_WINDOW = 120`（2 小时）
- 崩盘价追赶速度 `CRASH_CATCHUP_SPEED = 0.02`（2%）
- 崩盘价系数 `CRASH_PRICE_RATIO = 1.1`（近期最低的 1.1 倍）

**如果你在实际测试中发现崩盘频率偏离 24 小时**，调整以下两个参数即可：

| 调整方向                 | 修改参数                   | 效果                       |
| :----------------------- | :------------------------- | :------------------------- |
| 崩盘变慢（少于 1 次/天） | 增大 `CRASH_CATCHUP_SPEED` | 崩盘价追得更快             |
| 崩盘变快（多于 1 次/天） | 减小 `CRASH_CATCHUP_SPEED` | 崩盘价追得更慢             |
| 调整窗口大小             | 增大 `RECENT_WINDOW`       | 近期最低更"平滑"，崩盘变慢 |
| 调整追赶系数             | 增大 `CRASH_PRICE_RATIO`   | 目标更高，崩盘变快         |

---

## 三、给 Codex 的补充说明

如果 Codex 需要理解"为什么要这么设计"，可以附上这段：

> **设计意图**：
>
> 1. 崩盘价永远在"追赶"股价，但速度被限制在 2%/次，确保用户有反应时间。
> 2. 近期窗口（120 次）替代"历史最低"，确保崩盘价持续更新，不会卡住。
> 3. 崩盘后价格继续更新但冻结崩盘价/高位价，确保崩盘是"不可逆的死亡螺旋"。
> 4. 牛股取消时高位价向当前价靠拢 50%，给用户明确的"牛市结束"信号。
> 5. 均值回归基准固定 1.0，确保价格不会无限上涨，最终所有股票都走向崩盘。

---

## 四、如果你要微调

告诉我你想调整哪个参数，我帮你重新计算：

- 崩盘频率（现在是 24 小时）
- 追赶速度（现在是 2%）
- 近期窗口（现在是 120 次）
- 崩盘价系数（现在是 1.1）

Codex 拿到这份规则应该能直接写出可运行的代码。有问题随时说。
